import React from "react";

export default ({ children, meta }) => (
  <div className="flex container items-center justify-center h-32">
    <div className="text-grey-darker">© Example Blog</div>
  </div>
);
